{{- if not (eq .Values.storage.type "memory") }}

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ template "notary.fullname" . }}-notary-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.storage.pvcSize }}

{{- end }}